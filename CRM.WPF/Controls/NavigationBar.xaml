<UserControl x:Class="CRM.WPF.Controls.NavigationBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CRM.WPF.Controls"
             xmlns:assets="clr-namespace:CRM.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="#31407B"
             d:MinHeight="450" d:MinWidth="800"
             xmlns:navigator="clr-namespace:CRM.WPF.State.Navigators"
             
            >


    <Grid>
      
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
       

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
       
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Column="0" Grid.Row="0" >
          
     
            <Image Grid.Column="0" Source="/Controls/whiteLogo.png" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="2 5 2 30"/>
            <TextBlock Text="Felhasználónév" FontSize="20" Margin="10 0 0 0" Foreground="White"/>
            <TextBlock Text="Emailcím" FontSize="15" Margin="10 0 0 20" Foreground="White"/>
            <Button Grid.Column="0" x:Name="homeMenuBtn" Content="KEZDŐ OLDAL" Background="#31407B" FontSize="20" Foreground="White" Command="{Binding UpdateCurrentViewModelCommand}" CommandParameter="{x:Static navigator:ViewType.Home}" Click="Active" Padding="10"  BorderBrush="Transparent" />
            <Button Grid.Column="0"  x:Name="messageMenuBtn" Content="ÜZENETEK" Background="#31407B" FontSize="20" Foreground="White" Command="{Binding UpdateCurrentViewModelCommand}" CommandParameter="{x:Static navigator:ViewType.Message}" Padding="10"  BorderBrush="Transparent"    />
            <Button Grid.Column="0"   x:Name="taskMenuBtn" Content="FELADATOK" Background="#31407B"  FontSize="20" Foreground="White" Command="{Binding UpdateCurrentViewModelCommand}" CommandParameter="{x:Static navigator:ViewType.Task}" Padding="10"   BorderBrush="Transparent"   />
            <Button Grid.Column="0"  x:Name="overViewMenuBtn" Content="ÁTTEKINTÉS" Background="#31407B" FontSize="20" Foreground="White" Command="{Binding UpdateCurrentViewModelCommand}" CommandParameter="{x:Static navigator:ViewType.OverView}" Padding="10"   BorderBrush="Transparent"  />
            <Button Grid.Column="0"   x:Name="logOutMenuBtn" Content="KIJELENTKEZÉS" Background="#31407B" FontSize="20" Foreground="White" Command="{Binding UpdateCurrentViewModelCommand}" CommandParameter="{x:Static navigator:ViewType.LogOut}" Padding="10"  BorderBrush="Transparent" />
             

        </StackPanel>
        <TextBlock Grid.Column="0" Grid.Row="1"  Text="Version 1.02 " FontSize="10" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Bottom"  />
    </Grid>



  
</UserControl>
